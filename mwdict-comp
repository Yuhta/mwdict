# -*- mode: shell-script -*-
# Bash completion support for MWDict.

function _mwdict()
{
    local cur opts
    COMPREPLY=()
    cur="${COMP_WORDS[COMP_CWORD]}"
    opts=`mwdict-suggest $cur`

    COMPREPLY=( $(compgen -W "${opts}" ${cur}) )
    return 0
}

complete -F _mwdict mwdict
